server {
    listen                  100;
  
  location /authenticate {
        proxy_pass http://authservice:3000/api/authenticate;
    }

    location /post {
        proxy_pass http://postservice:3001/api/post;
    }
    
    location /story {
        proxy_pass http://storyservice:3002/api/story;
    }
}

1. change port for nginx default config file - /etc/nginx/sites-enabled (create in available then link in enabled)
2. set port in app config


Docker config
1. docker build . -t tasmia1128/name_as_wish
2. docker run -p port:defined_port tasmia1128/name_as_wish
3. images
4. docker ps 
5. docker log <container_id>
6. docker kill <container_id>

Docker Compose:
1. docker-compose build
2. docker-compose up
